export const validCpf = /^\d{11}$/;

export const validPhone = /^\d{10,11}$/;

export const validMedicalLicense = /^\d+-[A-Z]{2}$/;

export const lowerCaseLettersUnderscores = /^[a-z_]+$/;

export const timeRegex = /^([0-1]?[0-9]|2[0-3]):[0-5][0-9]$/;
